const FORM=document.getElementById("data-form");const baseProperties=["property","child","child-properties","multiple"];const baseObjects={input:()=>"",select2:()=>"","select2-tags":()=>"",datepicker:()=>"","datepicker-multiple":()=>"",};function getMetadata(){const properties=document.querySelectorAll("[property]");return Array.from(properties).map((element)=>{const data=element.getAttributeNames().reduce((acc,curr)=>{if(baseProperties.includes(curr))acc[curr]=element.getAttribute(curr);return acc;},{});return{...data,element};});}
function getExecutables(metadata){return metadata.map((meta)=>{if(_.has(meta,"multiple"))meta.value=[];else meta.value={};if(_.has(meta,"child")&&_.has(meta,"child-properties")){const libs=meta.element.getAttribute("child").split(",");const props=meta.element.getAttribute("child-properties").split(",");const objs=libs.map((lib,idx)=>({exec:baseObjects[lib],prop:props[idx],}));meta.exec=()=>{meta.value=objs.reduce((acc,curr)=>((acc[curr.prop]=curr.exec()),acc),{});};}
return _.pick(meta,["property","exec"]);});}
function generatePayload(){const metadata=getMetadata();const executables=getExecutables(metadata);console.log(executables);const payload=executables.reduce((acc,curr)=>((acc[curr.property]=curr.exec()),acc),{});console.log(payload);return{};}
function submit(){FORM.onsubmit=()=>{try{const payload=generatePayload();return false;}catch(error){return false;}};}
function bootstrap(){submit();}
bootstrap();